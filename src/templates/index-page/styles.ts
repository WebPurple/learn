import styled from 'styled-components';
import LogoLoader from '../../components/LogoLoader';

export const StyledMain = styled.main`
  padding: 5rem;
  width: 100%;
  text-align: center;
`;

export const StyledLoader = styled(LogoLoader)`
  width: 30rem;
  height: 30rem;
  overflow: visible;
`;
